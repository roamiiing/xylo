xylo:
  destinations:

  services:
    cheeseplate:
      source:
        type: pgsql
        config:
          username: labuser
          password: supersecure
          host: localhost
          port: 5432
      destination:
        - type: local
          config:
            path: /Users/roam/xylodumps/
